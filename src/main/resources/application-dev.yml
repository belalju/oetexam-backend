spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://belal-server.home:3308/oet_practice?useSSL=false&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:belal}
    password: ${DB_PASSWORD:Belalju@123}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: false

app:
  jwt:
    secret: ${JWT_SECRET:dev-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm}
    access-token-expiration-ms: 3600000
    refresh-token-expiration-ms: 604800000

logging:
  level:
    com.oet: DEBUG
